<h1><%= @patient.name %></h1>
<h3>Appointments: </h3>
<ul>
  <%= @patient.appointments.each do |a| %>
    <li><%= a.appointment_datetime.strftime("%B %e, %Y at %k:%M") %> <%= link_to(a.doctor.name, a.doctor) %></li>
  <% end %>
</ul>
