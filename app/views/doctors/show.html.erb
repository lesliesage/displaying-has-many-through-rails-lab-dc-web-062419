<h1><%= @doctor.name %></h1>
<h3><%= @doctor.department %></h3>
<h3>Appointments: </h3>

<ul>
  <%= @doctor.appointments.each do |a| %>
    <li><%= a.appointment_datetime.strftime("%B %e, %Y at %k:%M") %> <%= link_to(a.patient.name, a.patient) %></li>
  <% end %>
</ul>
